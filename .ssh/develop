#!/bin/bash
# Script to start development environment

echo "Starting development environment (Docker)..."
docker compose up -d dev

echo "Environment started! You can run './.ssh/test' or './.ssh/build' now."
# Optionally open a shell inside the dev container
# docker compose exec dev sh
