#!/bin/bash
# Script to run tests inside Docker

# Ensure container is running
CONTAINER_STATE=$(docker compose ps --status running -q dev 2>/dev/null)
if [ -z "$CONTAINER_STATE" ]; then
    echo "Starting development environment (Docker)..."
    docker compose up -d dev
fi

echo "Running tests inside the Docker environment..."
docker compose exec dev npm run test "$@"
